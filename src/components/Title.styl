.title
  background: linear-gradient(#ffffff,#ffffff, #333);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  margin 0
  font-size: 50px
  font-family: Montserrat

@media screen and (max-width 500px)
  .title
    font-size: 20px
